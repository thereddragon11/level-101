
class user:
def __inti__(sekf.name):
    self.names = name
    self.level = 1
    self.points = 0

def add_points(self. points):
    self.points += points
    self.check_level_up

def check_level_up(self):
    check_level_up =100 #  
    point required to level up
       white self.points>=
level_up_points * self.points:
    self.level+=1
    print(f"user: (self.name) .
     level up!  now at level
    (self.level)")
def get_status (self):
    return f"user: (self.name).
    level: (self.level), Points:
    (self.points)"
